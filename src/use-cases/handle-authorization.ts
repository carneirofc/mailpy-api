import { MailpyDB } from "../data-access/mailpy-db";

export default function makeHandleUserPermission({ mailpyDb }: { mailpyDb: MailpyDB }) {
  // const canUserControlGroups = async (user) => {
  //   // 1. get the user groups
  // // 2. get the
  // };
}
